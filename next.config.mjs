/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
        images: {
          domains: ['picsum.photos', 'chanrericr.com'],
        },
      
};



export default nextConfig;
